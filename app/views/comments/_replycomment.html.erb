<div class="comment-box">
	<%= error_messages_for :comment %>
	<% form_for :comment, :url => { :controller => "comments", :action => "create", :id => @post.id, :parent_id => parent } do |f| -%>
	<p>
	<%= f.text_field :content %></p>
	
	<p><%= submit_tag 'save' %></p>
	<% end -%>

</div>